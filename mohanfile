pipeline {
    agent any 
    stages{
        stage("git checkout code"){
            steps{
                git clone https://github.com/mohanpractice14242/actions.git
            }
        }
    }
}