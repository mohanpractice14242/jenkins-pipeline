pipeline {
    agent any
    stages {
        stage('Git Checkout Code') {
            steps {
                sh '''git clone https://github.com/mohanpractice14242/jenkins-pipeline.git
                    ls -l '''
                    
                
              
            }
        }
    }
}
