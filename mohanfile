pipeline {
    agent any
    stages {
        stage('Git Checkout Code') {
            steps {
                sh '''ls -l
                    '''
            }
        }
        stage("displaying the files"){
            steps{
                sh '''
                ls -l'''
            }
        }
        stage("getting the code "){
            steps{
                sh '''
                
                 ls -l
                 '''
            }
        }
        stage("clone the test cases repo"){
            steps{
                sh '''
                ls -l
                '''
            }
        }
    }
}
