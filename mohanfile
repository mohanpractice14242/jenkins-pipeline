pipeline {
    agent any
    stages {
        stage('Git Checkout Code') {
            steps {
                git branch: 'master', url: 'https://github.com/mohanpractice14242/actions.git'
                ls 
            }
        }
    }
}
